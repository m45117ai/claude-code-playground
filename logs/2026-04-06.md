# 📝 学習ログ — 2026-04-06

## ✅ 今日やったこと
- README.md を初心者向けに整理した
- README の `logs/` 説明が実装と合っているか確認した
- 保存の中心が `localStorage` で、`logs/` フォルダには自動保存していないことを確認した
- README の `logs/` 説明を、実装に合う表現へ修正した
- `git add` / `git commit` / `git push` を実行した
- push 時のエラー内容を確認し、`stash → pull --rebase → stash pop → push` の流れを理解した
- `git status` と `git diff learning-log.js` で未コミット変更を確認した
- タグ候補表示の表記ゆれ整理を確認した
- `GitHub` / `Claude Code` / `Notion` などのタグ候補表示がきれいにそろうことを確認した
- `learning-log.js` の変更をコミットして push した
- 空入力チェックを追加した
- 3項目すべて空のときは SAVE できず、メッセージが表示されることを確認した
- どれか1つでも入力があれば保存できることを確認した

## 💡 今日の作業で学んでほしかったこと
- README は見た目を整えるだけでなく、実装と一致していることが大事
- Git は「変更する → 確認する → コミットする → pushする」の流れで進めると安全
- push エラーは失敗ではなく、状況を整理すれば解決できることが多い
- `git status` は今の状態確認、`git diff` は変更内容確認に使える
- 機能追加では、実装前に仕様を決めて、実装後に画面で確認することが重要
- 1つの機能を「表示」「データ」「処理」に分けて考えると理解しやすい
- 「空のまま保存できない」ようにすることで、ツールは一段使いやすくなる

## 🎯 明日やったほうが良いこと
- 今回追加した空入力チェックをコミット・push して反映する
- README の機能一覧に「空入力では保存されない」を追記する
- `.DS_Store` を `.gitignore` に入れて、Git管理対象外にする
- 次の改善候補を1つだけ決める
  - タグ重複防止
  - export機能
  - READMEのスクリーンショット追加
- Notion に今日の学びを短く整理して残す
