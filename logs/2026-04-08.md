# 📝 学習ログ — 2026-04-08

## ✅ 今日やったこと
- `git status` で作業開始前の状態確認をした
- Claude Code を開いて、README.md に「空入力時は保存できない」機能を追記する依頼を送った
- README.md の追記内容を確認した
- `git add README.md` を実行した
- `git commit -m "READMEに空入力チェック機能を追記"` を実行した
- `git push` が失敗した理由を確認した
- `git pull --rebase` を実行して、GitHub側の変更を先に取り込んだ
- その後 `git push` を実行して、READMEの更新をGitHubに反映した
- ChatGPT macOSアプリの `Work with Apps` を設定した
- Chat Bar を使って、ターミナル連携が動作することを確認した
- `pwd` や `git status` の出力を、スクショなしで読める状態にできた
- Mac の名前を変更した
- ターミナルのテーマを `Grass` に変更した
- 今後は作業終了時に、ChatGPT のターミナル連携をオフにする運用に決めた

## 💡 今日の作業で学んでほしかったこと
- README の更新も、コードと同じように `add → commit → push` で管理すること
- `push` が失敗しても、`pull --rebase` で順番を整えれば進めることが多いこと
- ChatGPT のターミナル連携は、設定完了と実際に使える状態の確認が別だということ
- 作業環境は、見やすく整えるだけでも学習しやすさが上がること
- セキュリティは「便利だから常時オン」ではなく、使い終わったらオフにする運用も大事なこと

## 🎯 明日やったほうが良いこと
- 作業開始時に `git status` を確認する
- learning-log ツールの次の改善点を1つだけ決める
- 候補の中から1つ選んで着手する
  - タグ重複防止
  - export機能
  - READMEにスクリーンショット追加
- 作業終了時に ChatGPT のターミナル連携をオフにする
