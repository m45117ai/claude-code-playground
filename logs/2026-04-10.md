# 📝 学習ログ — 2026-04-10

## ✅ 今日やったこと
- `git status` と `git log --oneline -n 3` で作業開始時の状態確認をした
- learning-log の次の改善点として「タグ重複防止」を選んだ
- Claude Code に、タグ重複防止の調査を依頼した
- タグ保存の処理を確認し、保存時に重複削除する方針を決めた
- Claude Code で、保存時にタグ重複を除去する実装を入れた
- ブラウザで動作確認を行い、同じタグの重複は防げていることを確認した
- `learning-log.js` を `git add → git status → git commit → git push` で反映した
- その後 `git status` でローカルとリモートが同期されていることを確認した
- ChatGPT とターミナル連携の確認を行った
- アクセシビリティ権限まわりで少し詰まったが、最終的に連携できることを確認した
- 今後、連携が怪しいときは「まずアプリを再起動する」と決めた

## 💡 今日の作業で学んでほしかったこと
- 機能追加は、まず調査してから最小の実装に進むと分かりやすいこと
- タグ重複防止のような改善は、保存処理の1か所だけ直す形でも実現できること
- 動作確認では、実装できたかだけでなく「実際に使ってどうなるか」を見るのが大事なこと
- Gitでは、`git add` のあとに `git status` を見るセットが安全なこと
- 連携トラブルは、設定だけでなく再起動で直ることがあること

## 🎯 明日やったほうが良いこと
- 最初に `git status` で状態確認する
- タグ重複防止のREADME反映をするか決める
- 余裕があれば、タグの大文字小文字を統一するかどうか検討する
- 連携が必要なときは、最初にアプリ再起動で切り分ける

## ひとこと
今日は量よりも、開発の進め方とトラブル時の考え方を整理できた日だった。
進みが少なく見えても、こういう「詰まったときの対処」が後でかなり効いてくる。
